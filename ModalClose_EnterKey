// Close modal on Enter key
ele.outerFind('.loginX').on('keydown', function (event) {
  if (event.key === 'Enter' || event.keyCode === 13) {
    event.preventDefault();
    $(this).trigger('click');
  }
});
